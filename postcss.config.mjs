// @tailwindcss/postcss is the correct PostCSS plugin for Tailwind CSS v4.
// The old `tailwindcss` direct plugin was removed in v4.
const config = {
    plugins: {
        '@tailwindcss/postcss': {},
    },
};

export default config;
